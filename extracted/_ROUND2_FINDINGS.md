# Round-2 findings, grouped by file (for WF-4 fix agents)

Each agent: fix every item listed for YOUR file. Severities high>medium>low. Quote-evidence is in 'issue'. Suggested fixes are concrete and PDF-verified.


## docs/02_foundations/phase_vs_amplitude_noise.md  (2 findings)

1. [HIGH|citation] The page repeatedly cites "[P4] Fig. 3" as the figure that draws the ISF / APF / amplitude-decay-function relationship and the ideal-LC quadrature result (lines 102, 124, 181, 184: e.g. line 102 "[P4] Fig. 3 同時畫了理想 LC 振盪器的 ISF、APF、amplitude decay function 與三者關係"; line 181 "互為 quadrature（差 90°）——[P4] Fig. 3"). Verified against the PDF: [P4] Fig.3 (p.2124) is "Equivalence between an impulse train injection and a sinusoidal injection" — NOT the ISF/APF/decay figure. The ISF/APF/amplitude-decay figure is Fig.5 (p.2126): "Characterizing the effect that an instantaneous injection of charge has on an oscillator's amplitude deviations." This is exactly the fix mandated by _CORRECTION_SPEC.md C3 ("Replace '[P4] Fig.3' (for ISF/APF/quadrature) with '[P4] Fig.5, p.2126'"), which was applied to the Eq-number on line 123 but NOT to these four Fig.3 references — a round-1 regression.
   → FIX: Replace all four "[P4] Fig. 3" on lines 102, 124, 181, 184 with "[P4] Fig. 5, p.2126". E.g. line 102 → "[P4] Fig. 5, p.2126 同時畫了…"; line 181 → "…quadrature（差 90°）——[P4] Fig. 5, p.2126。"; line 184 source line → "…Fig. 5, p.2126".

2. [HIGH|citation] Lines 74-85 attribute the amplitude-decay closed form to a specific numbered equation that does not contain it. Line 74: "[P4] Eq.(25), p.2128 給出確切閉式" followed by d(t,φ)=e^{−t/τ0}, τ0=2Q/ω_osc; line 85: "$d(t,\phi)=e^{-t/\tau_0}$、$\tau_0=2Q/\omega_{osc}$ 出自 [P4] Eq.(25), p.2128，已逐字確認." Verified against the PDF (journal p.2128, Sec. III-F): Eq.(25) is Λ(ϕ)=τ0·Λ̃(ϕ) (the APF equals τ0 times the amplitude ISF). The expression d(t,ϕ)=e^{−t/τ0} where τ0=2Q/ω0 appears in the running prose of Section III-F immediately before Eq.(25), not as a numbered equation. _CORRECTION_SPEC.md C3 confirms: "decay d(t,φ)=e^{−t/τ₀}, τ₀=2Q/ω_osc, is in the same ideal-LC section p.2127–2128" — i.e. not labelled Eq.(25). The "已逐字確認 / verified verbatim" wording makes this a confidently-wrong citation.
   → FIX: Stop calling the decay form Eq.(25). On lines 74 and 85, cite it as prose, e.g. "[P4] Sec. III-F, p.2128 給出 ideal-LC 的特徵衰減函數 d(t,φ)=e^{−t/τ0}, τ0=2Q/ω0（緊接 Eq.(25) 之前的正文）". If a numbered equation is wanted, note that Eq.(25) is Λ(ϕ)=τ0·Λ̃(ϕ) (APF = τ0 × amplitude ISF), and the decay expression itself is unnumbered text on p.2128.


## docs/02_foundations/psd_phase_noise_jitter.md  (2 findings)

1. [HIGH|regression] Line 357 contains a leftover/garbled scratch term inside the period-jitter derivation's \begin{aligned} block: `=T^4\,\omega_0^{?}\ \cdots`. This introduces a spurious \omega_0 factor that does not belong in the expression at all (the very next line, 366, correctly shows \sigma_T^2 = T^4 \cdot 2\times10^{-10}\cdot(10^6)^2\cdot(9.9\times10^7) with NO \omega_0), and the literal `^{?}` superscript plus trailing `\cdots` is clearly unfinished/placeholder math that survived a round-1 expansion edit. It renders as a meaningless 'T⁴ω₀^? ⋯' line, breaking the step-by-step derivation a careful reader is meant to follow.
   → FIX: Replace the dangling third line of the aligned block. Either delete `=T^4\,\omega_0^{?}\ \cdots` so the block ends at the second line, or complete it to the actual simplified form, e.g. `=T^4\cdot 2\times10^{-10}(10^6)^2\!\int_{10^6}^{10^8}\!df` (consistent with line 356 and the result on line 366). The prose on line 361 already explains the (2\pi T)^2/(2\pi f_0)^2 = T^4 simplification, so no \omega_0 should appear.

2. [MEDIUM|regression] Line 480 (Example D, step 3) has an unresolved units placeholder: `後段 $=1.98\times10^{10}\ \text{Hz}^{?}$`. The `\text{Hz}^{?}` renders as a literal 'Hz^?' question-mark exponent — another leftover scratch marker in the same period-jitter worked example. (The factor 1.98e10 is 2\times10^{-10}\cdot(10^6)^2\cdot\int df, whose units are rad²/Hz·Hz²·... not simply 'Hz', so the placeholder was never legitimate.)
   → FIX: Drop the bogus unit superscript and state the value as a bare magnitude, e.g. `後段 $=1.98\times10^{10}$（rad²·Hz，與 $T^4$ 相乘後得 s²）`, or simply `後段 $=1.98\times10^{10}$`. The clean dimension accounting is already given correctly on line 488 ($T^4(\text{s}^4)\times(\text{rad}^2/\text{Hz})\times\text{Hz}=\text{s}^2$), so the inline `\text{Hz}^{?}` is both wrong and redundant.


## docs/02_foundations/dsp_view_of_phase_noise.md  (2 findings)

1. [HIGH|correctness] The Section-3 core code block (lines 114-115) assigns the result of apply_isf_weighting(...) directly to `phi` with the comment `# 2) ISF 加權 + 積分器：phi[k] = (dt/qmax) * cumsum( Gamma(w0 t) * i_n )`. But apply_isf_weighting in simulations/common/isf_utils.py (line 133-140) ONLY does the weighting: `return gamma_values * i_noise / qmax` — it performs NO cumsum and NO dt scaling. Its output is the integrand y[m]=Gamma·i_n/qmax, not the excess phase phi. So the displayed code does not compute phi as the comment claims, and it contradicts (a) the page's own discrete formula on line 93 phi[k]=(dt_s/qmax)·sum_m Gamma·i_n[m], (b) the page's prose `cumsum = 積分器` (line 101), and (c) the real lab_06 script (simulations/lab_06_white_noise_phase_noise.py line 60-63), which does the integration as a separate explicit step `g = gamma*i_n/qmax; phi = np.cumsum(g)*dt`. As written the example silently skips the integrator — the very block whose 1/(jω)→1/f² role the whole page is built around.
   → FIX: Either call the dedicated integrating helper that exists for exactly this — `phi = integrate_phase_from_noise(t, i_n, gamma_lc_ideal(2*np.pi*5e9*t), qmax=1e-12)` (its body is `np.cumsum(gamma_values*i_noise/qmax)*dt`, isf_utils.py line 130) — note it takes a precomputed gamma_values array, NOT a gamma_func kwarg; OR keep the two-step structure matching lab_06: `y = apply_isf_weighting(t, i_n, gamma_func=gamma_lc_ideal, qmax=1e-12, omega0=2*np.pi*5e9)` followed by `dt = 1/fs; phi = np.cumsum(y)*dt`. Update the comment so the cumsum/dt step is actually shown rather than hidden inside a function that does not do it.

2. [MEDIUM|regression] The script path is wrong on this page (twice): line 121 `完整 script：simulations/lab_06_white_noise_to_phase_noise.py` and line 143 `（simulations/lab_06_white_noise_to_phase_noise.py ...）`. The actual file is `simulations/lab_06_white_noise_phase_noise.py` (no `to_`). Every other page uses the correct name (figure_index.md:34, python_environment.md, white_noise_to_phase_noise.md:510, lab_06_white_noise_phase_noise.md, lab_09_design_tradeoffs.md:112). This page is the lone outlier — a dangling script reference. (The Docusaurus cross-links on lines 122 and 211 use the correct slug, so only the bare .py path strings are broken.)
   → FIX: Replace both occurrences of `simulations/lab_06_white_noise_to_phase_noise.py` with `simulations/lab_06_white_noise_phase_noise.py`.


## docs/03_isf_core_theory/white_noise_to_phase_noise.md  (2 findings)

1. [HIGH|correctness] The summary sentence concluding the down-conversion derivation (Step 3a, lines 149-150) states the noise being down-converted comes from 2nω₀: "**$2n\omega_0$ 處的 noise 被 ISF 第 $n$ 諧波搬到 $\Delta\omega$ 處，快項自動消失。**" This is physically wrong and directly contradicts the page's own Step (ii) two lines above (lines 139-140), where the $2n\omega_0$ component is explicitly identified as the FAST (sum-frequency) term whose amplitude is suppressed by ~Δω/(2nω₀) and gets averaged AWAY by the integrator. The injected tone is near $n\omega_0$ (specifically $n\omega_0+\Delta\omega$, line 102), and it is the noise near $n\omega_0$ — the difference-frequency / slow term — that survives and lands at $\Delta\omega$. Saying the surviving noise comes from $2n\omega_0$ inverts the mixer picture the whole subsection just built.
   → FIX: Change the load-bearing phrase to reference $n\omega_0$, e.g.: "**$n\omega_0$ 附近的 noise 被 ISF 第 $n$ 諧波搬到 $\Delta\omega$ 處（差頻慢項存活），而和頻 $2n\omega_0$ 的快項自動消失。**" This restores consistency with Step (ii) lines 139-140 ('快項 頻率約 $2n\omega_0$ ... 等效上被積分器平均掉').

2. [MEDIUM|pedagogy] In the rigorous Step C (lines 333-348), the page correctly computes $\frac{1}{2\pi}\int_0^{2\pi}\Gamma^2 dx = \frac{c_0^2}{4}+\frac12\sum_{n\ge1}c_n^2$ (line 334) and identifies this as $\Gamma_{rms}^2$ (line 342), then boxes $\sum_{n=0}^{\infty}c_n^2 = 2\Gamma_{rms}^2$ (line 348). But the connective prose (lines 337-339: '把 DC 寫成 $n=0$ 項並湊成「半個 $\sum_{n\ge0}c_n^2$」的形式 ... 補回 DC 的 half-weight') hand-waves over the one genuinely subtle point: the identity $\sum c_n^2=2\Gamma_{rms}^2$ is exact ONLY if the DC term is counted as $c_0^2/2$, NOT the naive $c_0^2+\sum_{n\ge1}c_n^2$. Verified numerically: with a random ISF, $c_0^2+\sum_{n\ge1}c_n^2$ overshoots $2\Gamma_{rms}^2$ by exactly $c_0^2/2$, whereas $c_0^2/2+\sum_{n\ge1}c_n^2=2\Gamma_{rms}^2$ holds. This is precisely the DC-double-count subtlety that correction C15 flagged in lab_05; the rigorous section here is the natural place to state it explicitly rather than bury it in 'half-weight' prose.
   → FIX: After line 348, add one explicit line, e.g.: '注意：這裡的 $\sum_{n=0}^\infty c_n^2$ 中 DC 項是以 $c_0^2/2$（半權重）計入的；若誤用整權 $c_0^2$ 會比 $2\Gamma_{rms}^2$ 多出 $c_0^2/2$（這正是 Parseval 對 $c_0/2$ 形式 DC 的記帳，見 rms_isf）。' This converts the confusing 'half-weight' aside into a precise, verifiable statement and aligns with C15.


## docs/03_isf_core_theory/lorentzian_linewidth.md  (2 findings)

1. [HIGH|regression] Line 233 is a live, malformed KaTeX display fence that renders a literal '?' placeholder to the student and is mathematically nonsensical. The block reads `$$\int_{-\infty}^{\infty}\frac{D}{D^2+\Delta\omega^2}\,\frac{d(\Delta\omega)}{2\pi}=\frac{1}{2\pi}\cdot\frac{D}{D}\cdot\pi\cdot\frac{1}{?}$$` — the RHS simplifies to (1/2)·(1/?), i.e. a dangling `\frac{1}{?}` with an undefined denominator, and the equation is left incomplete. This is a stray first-attempt fence that should have been deleted; the correct, clean integral (=π) is given immediately after on lines 238-239, making line 232-234 pure broken duplication. Renders visibly as a literal '1/?' in the published page.
   → FIX: Delete the entire broken block (lines 232-234, the `$$ ... \cdot\frac{1}{?}$$` fence) and the lead-in phrase that introduces it, so the prose flows directly from line 230 ('Lorentzian 的積分**收斂**：') into the correct standard-integral fence at line 236-239 ($\int_{-\infty}^{\infty}\frac{D}{D^2+\Delta\omega^2}d(\Delta\omega)=\pi$). The correct computation is already present and verified (∫ D/(D²+Ω²)dΩ = π), so no new math is needed.

2. [MEDIUM|regression] Example 2's '手感對照' (lines 401-404) anchors its comparison to the SSB value '-148 dBc/Hz' as the headline number, but the entire Lorentzian page computes D using the time-domain clean (/2) convention (boxed D=Γrms²S_i/(2qmax²), lines 273, 288-291, 424-425). Under that convention the self-consistent counterpart of canonical example B is -145 dBc/Hz (the parenthetical), NOT -148. The text then states '比例 1 的 -100 dBc/Hz 乾淨約 45 dB' and 'L 低 45 dB' — and 45 dB is exactly -100 minus -145 (the time-domain value), while -100 minus -148 would be 48 dB. So the dB-bookkeeping conclusion is correct only against the parenthetical -145, contradicting the headline -148. A careful student reconciling the numbers will hit a 3 dB mismatch. (Verified: linewidth ratio 1257/0.0398 = 3.16e4 = 45.0 dB; D ratio also 10^4.5.)
   → FIX: Lead with the time-domain value to match the convention the page actually uses: change '對應上一頁的 $\mathcal{L}(1\text{MHz})\approx-148$ dBc/Hz（時域版 $-145$）' to put -145 as the primary anchor (e.g. '對應上一頁時域乾淨版的 $\mathcal{L}(1\text{MHz})\approx-145$ dBc/Hz（SSB /4 慣例為 $-148$）'), so that the subsequent '乾淨約 45 dB' / '低 45 dB' arithmetic ( -100 − (−145) = 45 dB, matching the 45 dB linewidth ratio) is self-consistent with the headline number.


## docs/03_isf_core_theory/effective_isf.md  (3 findings)

1. [HIGH|pedagogy] The body "數值例子" toy (lines 188-198) is internally self-contradictory: the prose claims the device conducts "只在波谷附近" and frames the result as the benefit of leaking at an ISF-insensitive phase (line 214: "device 在不敏感相位才漏雜訊帶來的好處"), but the math places the NMF window at x=3π/2 where Γ=−sin(3π/2)=+1, i.e. |Γ|=1, the MAXIMUM-sensitivity phase. Line 196 even states the peak lands "在 |Γ| 接近其值的相位" (at the |Γ| peak). For Γ=−sin, the voltage extrema (x=π/2, 3π/2) are exactly where the ISF magnitude is largest, not smallest — so "電壓最低處" is NOT an insensitive phase. The resulting Γrms_eff=0.707·√0.1≈0.22 is the value for gating at the |Γ| PEAK; if it were truly at the ISF trough (the claimed Colpitts physics) it would be ~0.057, as worked Example 2(b) correctly computes. The body toy thus illustrates the WRONG alignment while narrating the good (Colpitts) alignment, directly conflicting with the page's own worked Example 1 (which honestly labels the same x=π/2-type gate as "最敏感"/"壞對齊").
   → FIX: Either move the body-toy window to an ISF trough (x=0 or x=π, where Γ≈0, giving Γrms_eff≈0.057 and matching the Colpitts narrative), and recompute L (~−175 dBc/Hz region); OR relabel the existing 0.22 toy honestly as gating at the |Γ| peak (a neutral/bad alignment) and drop the "在不敏感相位才漏雜訊" framing. As written, "假設 device 只在波谷附近導通 … α(x) 在 x=3π/2（−sin 為 +1、但電壓最低處）附近窄峰" plus the line-214 conclusion cannot both be true for Γ=−sin.

2. [MEDIUM|regression] Baseline mismatch in the "7 dB" improvement claim (lines 212-213). Γrms_eff≈0.22 was DERIVED by gating the true ideal-LC Γ=−sin (Γrms=0.707, stated on lines 194-195). But the improvement is then computed against canonical example B's Γrms=0.5: "相對例 B 的 −148.0 dBc/Hz（用未經閘控的 Γrms=0.5）… 改善約 7 dB（因為 Γrms_eff 從 0.5 掉到 0.22, 20log10(0.5/0.22)≈7.1 dB）". This silently swaps the 0.707 source the 0.22 came from for a 0.5 source. The self-consistent improvement vs the SAME −sin source is 20log10(0.707/0.22)≈10.1 dB (ungated −sin: L=−145.0; gated: −155.1; Δ=10.1 dB). Worked Example 2 (line 303) correctly uses the 0.707/−145 dBc baseline, so the body contradicts the page's own examples.
   → FIX: Compare against the same baseline the toy started from: "相對未閘控的 −sin LC（Γrms=0.707, L≈−145.0 dBc/Hz）改善約 10 dB（20log10(0.707/0.22)≈10.1 dB）". If a comparison to canonical example B (Γrms=0.5) is also wanted, state it as a separate note, not as the source of the 0.22→improvement, since 0.22 was not derived from 0.5.

3. [MEDIUM|regression] Half-applied round-1 edit in the "誠實聲明" block. Line 241 begins "**已補上**：Demir et al. 2000 … 正式 citation 見 [references] [E2]；" then ends with a verbless, predicate-less fragment "頁碼與 PPV 定義式，並核對 v₁(t) 與 Γ/q_max 的精確對應關係。" (line 242). This reads as a leftover "TODO: 補上頁碼與 PPV 定義式，並核對…" whose "TODO:" prefix was deleted but whose body was left dangling — so the sentence simultaneously claims "已補上" (done) and lists an un-actioned task. It also contradicts line 224, which still says the PPV/adjoint/Floquet sources are "標明出處待補" (to be supplied). The two lines give opposite verification states for the same external references.
   → FIX: Make the status consistent. Either: (a) if only the formal [E2] citation was added, rewrite line 241-242 as a clean sentence and keep an honest residual TODO, e.g. "**已補正式 citation**：Demir et al. 2000（IEEE TCAS-I 47(5):655–674, DOI 10.1109/81.847872）見 [references] [E2]。**TODO**：補上 PPV 定義式的精確頁碼，並核對 v₁(t) 與 Γ/q_max 的對應關係。"; and (b) reconcile line 224's "標明出處待補" with that status. As-is the trailing clause has no verb and is a malformed leftover.


## docs/05_paper_deep_dives/paper_002_jitter_phase_noise_ring.md  (2 findings)

1. [HIGH|citation] Key-figures table row for Fig. 5 (line 184) is wrong on BOTH the page number and the content. Rendering [P2] confirms: p.792 (idx2) contains only Fig. 3 (Clock jitter increasing with time) and Fig. 4 (RMS jitter vs measurement time, log-log) — there is NO Fig. 5 on p.792. Fig. 5 is on p.793 and its caption is 'ISF for ring oscillators of the same frequency with different number of stages' (it overlays the ISF for N=3, 7, 15 — a multi-N comparison). The table instead labels Fig. 5 as '792 | single-ended ring 單級的波形與 ISF（能量集中在 transition）| toy 三角 ISF（lab_03）'. That 'approximate waveform + triangular ISF concentrated at transitions' description actually matches Fig. 6 ('Approximate waveform and ISF for ring oscillator', p.793), not Fig. 5. A cross-cutting lens cannot catch this because the page number is internally self-consistent (it never appears elsewhere). This mislabels which figure backs the toy triangular-ISF claim.
   → FIX: Split into two correct rows. Replace the single line `| Fig. 5 | 792 | single-ended ring 單級的波形與 ISF（能量集中在 transition） | toy 三角 ISF（lab_03） | ⚠️ |` with rows reflecting the PDF captions: `| Fig. 5 | 793 | 同頻、不同級數 N（3/7/15）的 ring ISF 對照（峰隨 N↑ 變矮） | lc_vs_ring 的 N-scaling 論證 | — |` and `| Fig. 6 | 793 | ring 的近似波形與三角 ISF（能量集中在 transition） | toy 三角 ISF（lab_03） | — |`. Verified caption text from render of jr_p793.png: 'Fig. 5. ISF for ring oscillators of the same frequency with different number of stages.' and 'Fig. 6. Approximate waveform and ISF for ring oscillator.'

2. [MEDIUM|regression] The C9 fix (f₀ citation Eq.(14)→Eq.(15)) did NOT land on this page. The section at lines 118-120 reads '### Eq.(14)：ring 頻率與級數' and '**Original formula**（[P2] Eq.(14), p.794）' for $f_0=\frac{1}{2N\tau_D}$. The PDF p.794 (rendered/extracted) shows Eq.(14) is the stage-delay relation (t̂_D=η/f'_max) and Eq.(15) is 'The period is 2N times longer than a single stage delay' → f₀=1/(2Nτ_D). _AUTHORING_SPEC item 21 and _CORRECTION_SPEC C9 both confirm the f₀ formula must be cited as [P2] Eq.(15), p.794. The page is even internally inconsistent: line 144 already correctly attributes the period relation $2\pi=2N\eta/f_{max}$ to Eq.(15) while the header/formula above still credits Eq.(14).
   → FIX: Change the header '### Eq.(14)：ring 頻率與級數' → '### Eq.(15)：ring 頻率與級數' and the attribution '**Original formula**（[P2] Eq.(14), p.794）' → '**Original formula**（[P2] Eq.(15), p.794；Eq.(14) 定義正規化級延遲 $\hat t_D=\eta/f'_{max}$）', matching _AUTHORING_SPEC item 21 and C9. Leave line 144's 'Eq.(14)–(15)' as is — it is already correct.


## docs/06_design_insights/device_noise_mapping.md  (2 findings)

1. [HIGH|regression] The Step 3 'cyclostationary' toy-code block (lines 119-129) calls `effective_isf(gamma, alpha=0.5)` with the keyword `alpha`, but the real function signature in simulations/common/isf_utils.py:202 is `effective_isf(gamma_values, alpha_values)`. Running the snippet verbatim raises `TypeError: effective_isf() got an unexpected keyword argument 'alpha'`. The page explicitly invites the reader to run it ('可用內建函式做 toy 計算') and even reassures '`effective_isf` 與 `gamma_rms` 為真實函式' (line 131), so a copy-paste run fails. This is the same class of broken-call bug that C14 fixed elsewhere (wrong kwarg).
   → FIX: Change `gamma_eff = effective_isf(gamma, alpha=0.5)` to the positional form `effective_isf(gamma, 0.5)` (verified to run: gives c0=-0.0, Gamma_rms=0.3536) or use the correct keyword `alpha_values=0.5`.

2. [LOW|pedagogy] Subtle mismatch between the prose's central cyclostationary lesson and the toy code's chosen alpha. The prose (lines 111-113) stresses that an ASYMMETRIC alpha can 'regrow' a nonzero c0 even when the bare Gamma has c0=0. But the code uses a CONSTANT alpha=0.5, which merely scales Gamma uniformly and yields c0=0 (verified). A reader who runs it sees c0=-0.0 / Gamma_rms=0.354 and gets the opposite intuition (constant gating cannot create c0).
   → FIX: After fixing the kwarg, consider using a phase-dependent NMF window (e.g. an `alpha` array that is ~1 over half the period and ~0 over the other half) so the printed c0 comes out nonzero, matching the 'asymmetric alpha regrows c0' point made in the bullet at lines 111-113. Minor; the snippet is already labeled toy.


## docs/06_design_insights/lc_vs_ring.md  (3 findings)

1. [HIGH|correctness] Line 125 reproduces [P2] Eq.(12) for the accumulated-jitter constant kappa INCORRECTLY: it writes the 'verified' verbatim form as kappa = (Gamma_rms/q_max)*sqrt((i_n^2/df)/2), which is MISSING the omega_0 in the denominator. I verified Eq.(12) directly against the PDF (jitter_ring.pdf, journal p.793): the paper reads kappa = (Gamma_rms/(q_max*omega_0))*sqrt((1/2)*(i_n^2/df)). The omission is not cosmetic: it makes the formula dimensionally WRONG and self-contradictory with the page's own unit check on line 124. The stated form has units (1/C)*sqrt(A^2/Hz) = (1/C)*A*sqrt(s) = 1/sqrt(s), i.e. the INVERSE of the required sqrt(s) that line 124 asserts. Only with the omega_0 (units 1/s) does kappa come out as sqrt(s). The leading proportionality 'kappa^2 ∝ Gamma_rms^2/q_max^2 · i_n^2/df' on the same line likewise drops the 1/omega_0^2.
   → FIX: Correct the verbatim Eq.(12) on line 125 to include omega_0: '已核實：$\kappa=\dfrac{\Gamma_{rms}}{q_{max}\,\omega_0}\sqrt{\tfrac12\,\overline{i_n^2}/\Delta f}$（[P2] Eq.(12), p.793）'. Correspondingly fix the proportionality to '$\kappa^2\propto\dfrac{\Gamma_{rms}^2}{q_{max}^2\,\omega_0^2}\cdot\overline{i_n^2}/\Delta f$'. This restores the sqrt(s) units claimed on line 124.

2. [MEDIUM|regression] Round-1 fix C1 (every −89.2 ring-FOM value → −91.0) was applied to Example 1 (lines 175/179 correctly say −91.0, and line 181 correctly states the gap is '~57 dB') but NOT to Example 2. Line 212 still reads: '理想單源下 LC $-148$ vs ring $-89$ → LC 約好 59 dB'. The ring −89 and the 59 dB gap are stale (−148−(−89)=−59). With the corrected ring value −91.0, the gap is −148−(−91)=−57 dB. As written, Example 2 contradicts Example 1 on the same page.
   → FIX: On line 212 change 'ring $-89$ → LC 約好 59 dB' to 'ring $-91$ → LC 約好 57 dB' (and the recap on line 181 already uses 57 dB, so this also restores intra-page consistency).

3. [LOW|citation] Line 28 attributes the *triangular* toy ISF shape to '[P2] Fig. 5'. Verified against the PDF: [P2] Fig. 5 (p.793) shows the ACTUAL simulated ring-oscillator ISF curves for N=3,5,...,15 (peaked, asymmetric, NOT triangular). The *triangular approximation* ('Approximate waveform and ISF for ring oscillator') is [P2] Fig. 6 (p.793). The page text 'ISF 用 triangular（三角形）toy 形狀 ... （[P2] Fig. 5）' therefore points at the wrong figure for the triangular shape specifically.
   → FIX: On line 28 change the citation for the triangular shape to '[P2] Fig. 6'（or cite both: 'actual ISF [P2] Fig. 5, 三角形近似 [P2] Fig. 6'）. Line 37's '見 [P1] Fig. 7' is a separate citation and is fine.


## docs/06_design_insights/quadrature_and_coupled_oscillators.md  (2 findings)

1. [HIGH|correctness] The QVCO I/Q phase-error formula has the Q-dependence INVERTED. Section 2 states `$$\Delta\phi_{IQ}\ \approx\ \frac{1}{2m\,Q}\,\frac{\Delta\omega_0}{\omega_0}$$` (line 136) plus prose 'tank 越尖（$Q$ 大），同樣失配下 I/Q 相位誤差越小' (line 140) and '$m,Q$ 越大誤差越小' (line 136). This contradicts the Adler/injection-locking framework the page itself uses in Section 4. The mutual-coupling lock range is the Adler form $\omega_L=\tfrac12 I_{inj}|\tilde\Gamma_1| = \frac{m\,\omega_0}{2Q}$ (verified: [P3] Eq.(35), p.2114; and the bulk-coupled-QVCO literature gives pull $\propto \frac{f_0}{2Q}\tan^{-1}(I_C/I_M)$). A static tank detuning $\Delta\omega_0$ then gives $\Delta\phi_{IQ}\approx \Delta\omega_0/\omega_L = \frac{2Q}{m}\frac{\Delta\omega_0}{\omega_0}$ — i.e. phase error is PROPORTIONAL to Q (a higher-Q tank has a NARROWER lock range, so the same fractional mismatch produces a LARGER phase error), not $\propto 1/Q$. The $1/m$ part is correct; only the Q factor is wrong. The error propagates to the worked example (line 144: $\frac{1}{2\times0.3\times10}\times0.01\approx0.10^\circ$ — with the correct $2Q/m$ form this is $\approx 38^\circ$, a factor $Q^2=100$ off; a real QVCO with $Q=10,m=0.3$ and 1% tank mismatch does NOT achieve 0.1°, which is the tell), the design bound '$mQ\gtrsim 0.57$' (line 148), the Section-4 cross-reference '$\Delta\phi_{IQ}\propto 1/(mQ)$ 的微分方程版本' (line 275), and the failure-conditions table 'I/Q error $\propto 1/(mQ)$' (line 303).
   → FIX: Replace the $1/(mQ)$ Q-dependence with the Adler-consistent $\propto Q/m$ in all six spots. Formula (line 136): `$$\Delta\phi_{IQ}\ \approx\ \frac{2Q}{m}\,\frac{\Delta\omega_0}{\omega_0}$$` (or equivalently $\Delta\omega_0/\omega_L$ with $\omega_L=m\omega_0/(2Q)$). Fix the prose to '耦合越強（$m$ 大）誤差越小，但 tank 越尖（$Q$ 大）lock range 越窄、同樣失配下 I/Q 誤差越大'. Recompute line 144 ($\frac{2\times10}{0.3}\times0.01\approx0.67$ rad $\approx 38^\circ$ — and pick more realistic inputs, e.g. much smaller mismatch, so the worked number reflects a sane QVCO). Recompute the design bound on line 148. Update lines 275 and 303 to $\propto Q/m$.

2. [MEDIUM|regression] The '記帳口訣' (mnemonic) blockquote at line 218 contains a broken/dangling parenthetical: '相關 ↔ 振幅相加（功率 $\times N^2$ 除以 …）→ 不享受平均'. The clause '功率 $\times N^2$ 除以 …' trails off with an ellipsis and an unfinished '除以' (divided by …), reading like a sentence that was cut off mid-edit. A reader cannot tell what the power is divided by.
   → FIX: Complete or drop the fragment. Intended meaning: for N fully-correlated sources the amplitudes add, so total power scales as $N^2$ while per-source power is unchanged → no $1/N$ averaging benefit. Rewrite e.g. '相關 ↔ 振幅相加（總功率 $\propto N^2$，相對單源不下降）→ 不享受平均；不相關 ↔ 功率相加（總功率 $\propto N$）→ 享受 $-10\log_{10}N$' and remove the trailing '除以 …'.


## docs/99_appendix/derivation_floquet_ppv.md  (2 findings)

1. [HIGH|regression] Line 131 still cites the stale '[P4] Fig. 3' for the amplitude decay function: «擾動會被拉回 limit cycle——這就是「振幅擾動會衰減」（[P4] Fig. 3 的 amplitude decay function …）». This is exactly the round-1 C3 fix that did NOT land on this appendix page. Verified against the [P4] PDF: Fig. 3 caption is 'Equivalence between an impulse train injection and a sinusoidal injection' (the impulse-train↔sinusoid equivalence, journal p.2124), while the amplitude-decay figure is Fig. 5 — caption 'Characterizing the effect that an instantaneous injection of charge has on an oscillator's amplitude deviations. (a) Decay of the excess amplitude … (b) Decay of the excess energy …', on journal p.2126. So the cited figure is wrong.
   → FIX: Replace «[P4] Fig. 3 的 amplitude decay function» with «[P4] Fig. 5, p.2126 的 amplitude decay function» (per C3; verified: Fig.5 p.2126 is the amplitude-decay characterization, Fig.3 is the impulse-train/sinusoid equivalence).

2. [MEDIUM|citation] Line 51 cites the ΔV=Δq/C relation as '[P1] Eq.(9), p.182': «（[P1] Eq.(9), p.182 的 $\Delta V=\Delta q/C_{node}$ 的微分版）». Verified against general.pdf: Eq.(9) ('The instantaneous voltage change ΔV is given by (9), where ΔQ is the total injected charge … and C is the total capacitance at that node') sits at the bottom of journal p.181 (the same column/page as Fig.4, which round-1 C10 already corrected to p.181). Eqs.(10) and (11) are the ones on p.182. So the Eq.(9) page anchor is off by one — same error class as C10.
   → FIX: Change «[P1] Eq.(9), p.182» to «[P1] Eq.(9), p.181» (Eq.(9) ΔV=Δq/C is on journal p.181, consistent with C10's Fig.4→p.181; only Eqs.(10)/(11) are on p.182, which the rest of this page cites correctly).


## docs/02_foundations/lti_vs_ltv.md  (1 findings)

1. [MEDIUM|pedagogy] The slope heuristic in Section 3 (lines 87-89) is internally self-contradictory and physically backwards. It states 相位偏移 ≈ 「電壓跳變 ÷ 此刻波形斜率」 and then 斜率大（零交越）→ 同一個 ΔV 等效於更大的時間（相位）位移. Under that Δt = ΔV/slope picture, a LARGER slope gives a SMALLER time shift (Δt = ΔV/|dV/dt|), so the heuristic actually predicts maximum sensitivity at the PEAK (slope→0, Δt→∞) and minimum at the zero crossing — the exact INVERSE of the true ISF. It then concludes 'Γ 跟波形斜率高度相關，對正弦波就是 -sinθ' (Γ ∝ slope), which contradicts the 1/slope dependence its own ΔV/slope wording implies. I verified numerically: for V=cosθ, the ΔV/slope heuristic gives |Δφ| diverging at the peak/trough and minimal at the ZC, opposite to Γ=-sinθ. This also clashes with the prerequisite oscillator_phase.md 第4步 (lines 123-126), which this page claims to continue (line 87 '接 oscillator_phase 第 4 步') but which actually uses the correct TANGENTIAL-PROJECTION argument (a fixed voltage kick along the v-axis is purely tangential at the ZC where the state velocity is along v → all phase; purely radial at the peak → all amplitude), giving Γ ∝ sinθ. The location conclusion (max at ZC) happens to be right, but the stated mechanism teaches the wrong functional relationship and wrong direction.
   → FIX: Replace the ΔV/slope framing with the correct projection mechanism used on oscillator_phase.md. E.g.: '相位偏移大小取決於電壓跳變投影到 limit cycle 切線方向的分量。零交越時狀態速度（切線）正好沿著電壓軸，固定的 ΔV 幾乎全部變成切向位移 → Δφ 最大；波峰時切線垂直於電壓軸，ΔV 幾乎全是徑向（純振幅）→ Δφ≈0。切向分量 ∝ sinθ，故對正弦波 Γ(θ)=-sinθ。' Drop the 'ΔV ÷ 斜率 → 斜率大等效更大時間位移' sentence, which gives Γ ∝ 1/slope (divergent at the peak) and is the inverse of the true ISF.


## docs/02_foundations/stochastic_noise_basics.md  (1 findings)

1. [MEDIUM|citation] All cyclostationary citations on the page (the page's headline topic) cite [P1] p.185, but Section III-D "Cyclostationary Noise Sources" and Eqs.(25),(26),(27) plus the LC-vs-ring discussion are on PRINTED PAGE 186, not 185. Verified by extracting general.pdf: PDF page index 6 = printed p.185 (contains Eqs.19-24, incl. the device-flicker Eq.(22)); PDF page index 7 = printed p.186 (begins with "D. Cyclostationary Noise Sources", Eqs.(25)-(27), and the "This unfortunate coincidence..." sentence). This directly contradicts correction-spec C6 which says to cite cyclostationary as "p.186" — i.e. a round-1 correction that did not land on this page. Affected lines: 127 ([P1] Sec. III-D, p.185), 131 (cyclostationary quote, p.185), 137 (Eq.(25), p.185), 158 (Eq.(27), p.185), 167 (effective-ISF quote, p.185), 179 (LC-vs-ring examples, p.185), 191 ("unfortunate coincidence" quote, p.185), 238 (summary). NOTE: line 56's "Eq.(22), p.185" is CORRECT and must be left alone (Eq.22 is genuinely on p.185); only the cyclostationary refs are wrong. Also note the page's section label "Sec. III-D" is CORRECT (the PDF section is "III. MODELING OF PHASE NOISE", subsection D) — do not change III to II despite C6's wording; only the page number is wrong.
   → FIX: Change "p.185" → "p.186" on lines 127, 131, 137, 158, 167, 179, 191. On line 238, split the conflated citation: "來源：[P1] Sec. III-D（cyclostationary）Eqs.(25)–(27), p.186；device flicker Eq.(22), p.185；claims C9." — because Eq.(22) is on p.185 but Eqs.(25)/(27) are on p.186, the current "Eqs.(22),(25),(27)；p.185" mislabels two different pages under one number. Leave line 56 (Eq.(22), p.185) unchanged.


## docs/03_isf_core_theory/isf_definition.md  (1 findings)

1. [MEDIUM|citation] Line 134 cites Fig. 4 to the wrong journal page: "**怎麼讀這個 $-\sin\theta$**（對上 [P1] Fig. 4 的直覺，p.182）". Verified against the [P1] PDF: the Fig. 4 caption ("(a) Impulse injected at the peak, (b) impulse injected at the zero crossing, and (c) effect of nonlinearity ... in state-space") sits on journal p.181 (PDF index 2), not p.182. The state-space/peak-vs-zero-crossing prose continues onto p.182, but the figure itself is p.181. This is the exact fix prescribed by correction spec C10 ("Fix p.182→p.181") that did NOT land on this page. (Note: the Eq.(9)/(10)/(11) citations to p.182 on lines 12, 18, 33, 273 are all CORRECT and verified — only the Fig. 4 page is wrong.)
   → FIX: Change line 134 "（對上 [P1] Fig. 4 的直覺，p.182）" to "（對上 [P1] Fig. 4 的直覺，p.181）". Per C10, Fig. 4 is on p.181 (Fig. 6 is p.182, Fig. 7 is p.183). Line 148's unpaginated "[P1] Figs. 4, 6, 7(a)" can stay as-is since it carries no page number.


## docs/03_isf_core_theory/impulse_to_phase_shift.md  (1 findings)

1. [MEDIUM|citation] Eq.(9) ΔV=Δq/C_node is cited as p.182 on this page (line 39: "（[P1] Eq.(9), p.182）" and line 169 重點回顧: "[P1] Eqs.(9),(10),(11)，p.182"). Verified against the PDF: Eq.(9) actually appears on journal p.181 (PDF index 2 — the section 'The instantaneous voltage change ... is given by (9)' is at the bottom of p.181), while only Eq.(10) (h_φ) and Eq.(11) (superposition integral) are on p.182 (PDF index 3). So lumping all three under 'p.182' is factually off by one page for Eq.(9). Note this mirrors the AUTHORING_SPEC formula table (its item 2 also writes 'Eq.(9), p.182'), and round-1 C10 only moved Fig.4 to p.181, leaving the Eq.(9) page citation unaddressed — so this is a residual citation inaccuracy, not a fresh regression.
   → FIX: Split the page numbers: cite Eq.(9) as p.181 and Eq.(10)–(11) as p.182. Concretely, change line 39 '（[P1] Eq.(9), p.182）' → '（[P1] Eq.(9), p.181）', and change the 重點回顧 line 169 '來源：[P1] Eqs.(9),(10),(11)，p.182' → '來源：[P1] Eq.(9) p.181、Eqs.(10),(11) p.182'. (Lines 83 and 100 cite Eq.(10)/(11) as p.182, which is correct and need no change.)


## docs/03_isf_core_theory/convolution_derivation.md  (1 findings)

1. [MEDIUM|pedagogy] Example 2's half-period transient is reported as φ ≈ -636619.7 rad (verified: analytic φ(T/2) = I0/qmax·(-2)/ω0 = -636619.77 rad), an absurd ~6×10^5 rad 'phase'. The text frames it only as '非零暫態' without any caveat that this enormous magnitude is a pure normalized-units artifact of choosing I0=2 µA against qmax=1 pC (a 2×10^6 ratio) over a half-period of t=0.5 s in normalized time. A reader doing a physical sanity check will be confused that a 'phase' came out to ~10^5 radians while every other example on the page is in mrad. The small-signal/linearity premise the whole page rests on (第1步 d τ≪T, 'charge up to 10% of total' per [P1] p.182) is grossly violated here, yet the page invokes this same example as the time-domain proof of DC suppression.
   → FIX: Add one sentence after '會留下非零的暫態相位偏移' noting the magnitude is a normalized-units artifact and not a physical radian count, e.g. '（注意：此處 I0=2 µA、qmax=1 pC、半週期 t=0.5 s 為 normalized 數值，故 φ 數值極大；重點在「整週期抵消、半週期不抵消」的符號結構，不是這個 rad 數本身的物理大小）'. This keeps the pedagogical point (DC/symmetric-ISF cancellation) while preventing a false dimension/sanity check.


## docs/03_isf_core_theory/fourier_series_of_isf.md  (2 findings)

1. [MEDIUM|regression] Line 122 cites the [P2] symmetry-voltage figure as "[P2] Fig. 17, p.800". Correction-spec C5 (HIGH) requires this to be p.802, and I verified directly against jitter_ring.pdf: Fig. 17 "Phase noise versus symmetry voltage for oscillator number 7" is on journal page 802 (PDF page index 12, running header "802"); page 800 (idx 10) contains only tables. The round-1 C5 fix (p.800 -> p.802) did not land on this page, leaving a stale/wrong page citation that contradicts the single-source-of-truth correction spec.
   → FIX: On line 122 change "（[P2] Fig. 17, p.800 有實驗佐證）。" to "（[P2] Fig. 17, p.802 有實驗佐證）。" per correction-spec C5 (verified: Fig.17 is on p.802).

2. [LOW|pedagogy] The step-7 symmetry table (line 236) labels the toy `gamma_asymmetric` row as "rise/fall 不對稱（toy gamma_asymmetric）" giving c0=2alpha. But isf_utils.py defines gamma_asymmetric(theta,alpha)=cos(theta)+alpha (verified in the docstring: "Gamma(theta) = cos(theta) + alpha ... c0 (= 2*alpha here)"). That is a pure DC offset of an even cosine, NOT a rise/fall (transition-slope) asymmetry; calling it "rise/fall 不對稱" misattributes the physical mechanism that lifts c0. Step 8 (line 278) and Figure-three correctly describe it as just cos(theta)+alpha, so the table label is the only mismatch.
   → FIX: Relabel the line-236 table cell from "rise/fall 不對稱（toy gamma_asymmetric）" to something matching the actual model, e.g. "DC 偏移 cos(theta)+alpha（toy gamma_asymmetric）" — keeping the c0=2*alpha conclusion. (Optional: add one line noting real rise/fall slope asymmetry is a distinct mechanism that also raises c0.)


## docs/03_isf_core_theory/flicker_noise_upconversion.md  (3 findings)

1. [MEDIUM|regression] [P2] Fig.17 citation still says "p.800" in three places (lines 199, 203, 273) — the Round-1 correction C5 ("p.800 → p.802") closed the ⚠️ markers but did NOT land the page-number fix on this page. Verified against jitter_ring.pdf: Fig.17 "Phase noise versus symmetry voltage for oscillator number 7" is printed on p.802 (PDF idx 12), and p.800 (idx 10) is TABLE III / Fig.15 power-splitter measurement — unrelated.
   → FIX: Change all three "p.800" → "p.802" for the Fig.17 symmetry-experiment reference: line 199 "[P2] Fig. 17, p.800", line 203 "(p.800 附近原文)", line 273 "[P2] Fig. 17, p.800". (Fig.17 verified on p.802.)

2. [MEDIUM|citation] The "differential symmetry is insufficient" + "more linear loads (resistors / linearized MOS)" + tail-source limitations are attributed to "[P2] Sec. V (p.800 附近)" (lines 203, 205-206, 273). Verified against jitter_ring.pdf: this discussion is in Sec. VII DESIGN IMPLICATIONS on p.798 (PDF idx 8) — verbatim "As shown in [16], differential symmetry is insufﬁcient, and the symmetry of each half circuit is important. One practical method ... is to use more linear loads, such as resistors or linearized MOS devices." Sec. V of [P2] is "EXPRESSIONS FOR JITTER AND PHASE NOISE" (p.795), not the symmetry-design discussion.
   → FIX: Replace "[P2] Sec. V（p.800 附近）" / "(p.800 附近原文)" with "[P2] Sec. VII (Design Implications), p.798". The "differential symmetry is insufficient…symmetry of each half circuit is important" and "more linear loads (resistors / linearized MOS)" quotes are both on p.798.

3. [MEDIUM|correctness] The "核心 Python" snippet (lines 248-257) is broken and diverges from the real script simulations/lab_07_flicker_noise.py, violating the authoring rule "引用真實函式，勿杜撰". (1) `lambda th: gamma_asymmetric(0.5)(th)` raises `TypeError: 'numpy.float64' object is not callable` — gamma_asymmetric(theta, alpha=0.3) takes the ANGLE as first arg and returns a scalar, not a callable. (2) It imports/uses `apply_isf_weighting`, but the real lab_07 uses a local `phase_from_isf(...)` with `np.cumsum` and does NOT import apply_isf_weighting. (3) `t` is passed to apply_isf_weighting but never defined in the snippet (real script: `t = np.arange(n)/fs`).
   → FIX: Mirror the real script: import `gamma_symmetric, gamma_asymmetric` (drop apply_isf_weighting), define `t = np.arange(n)/fs; theta = ...`, and call `gamma_asym = gamma_asymmetric(theta, alpha=0.5)  # c0 = 2*alpha = 1.0`, then weight via the same cumsum step the real lab uses. As written, copy-pasting the snippet errors out.


## docs/03_isf_core_theory/capstone_lc_end_to_end.md  (2 findings)

1. [MEDIUM|regression] Station-⑤ heading, frontmatter description, and mermaid node still advertise the main-spine station-⑤ output as −148 dBc/Hz, but the section's own derivation computes and forwards −145 dBc/Hz (the true-LC Γ_rms=1/√2 value). Line 206 heading literally reads '站⑤：代 [P1] Eq.(21) 得 ... (canonical 例 B，$-148$ dBc/Hz)' over a section whose result (line 248) is $\mathcal{L}(1\,\text{MHz})=...=-145.0$ dBc/Hz; line 3 description says '→ S_φ/L(Δω)（−148 dBc/Hz）'; line 30 mermaid node S4 says '[P1] Eq.(21) → −148 dBc/Hz'. The body (251-254, 268, 472-473, 498) correctly treats −145 as this page's spine value and −148 as only canonical-example-B's representative-Γ_rms value. A reader following the spine is told the output is −148 in the heading/diagram/abstract but every downstream station (⑥ D, ⑦ jitter) is driven by Γ_rms²=0.5 → −145. Likely a round-1 −145/−148 reconciliation that updated the body but not the heading/description/mermaid.
   → FIX: Relabel so the spine output reads −145 (true LC) with −148 as the parenthetical canonical cross-note, not the headline. E.g. line 206 heading: '站⑤：代 [P1] Eq.(21) 得 $S_\phi/\mathcal{L}(\Delta\omega)$（真·LC $-145$ dBc/Hz；規範例 B $-148$，差 3 dB）'; line 30 mermaid: '[P1] Eq.(21) → −145 dBc/Hz'; line 3 description: 'S_φ/L(Δω)（真·LC −145 dBc/Hz）'. Keep the −148 cross-note where it already appears in the body.

2. [MEDIUM|correctness] The station-④ summary-table cell (line 442) writes the Parseval relation as a single broken equality chain: '$\Gamma_{rms}=\sqrt{\tfrac1{2\pi}\int\vert\Gamma\vert^2}=2\Gamma_{rms}^2=\sum c_n^2$'. This equates $\Gamma_{rms}$ ($=1/\sqrt2\approx0.707$) with $2\Gamma_{rms}^2$ ($=1$) and $\sum c_n^2$ ($=1$), which is false ($0.707\ne1$). The correct identity (stated correctly in the body, line 189-192, and matching extracted [P1] Eq.(20): $\sum c_n^2=\frac1\pi\int_0^{2\pi}|\Gamma|^2dx=2\Gamma_{rms}^2$) is that $\Gamma_{rms}=\sqrt{\frac1{2\pi}\int|\Gamma|^2}$ AND separately $2\Gamma_{rms}^2=\sum c_n^2$ — the table conflates the rms definition with the Parseval identity into one invalid '=' string.
   → FIX: Split the conflated chain in the table cell so the two distinct relations don't share an '=' . E.g. '$\Gamma_{rms}=\sqrt{\tfrac1{2\pi}\int\vert\Gamma\vert^2dx}$; $\;2\Gamma_{rms}^2=\sum c_n^2$' (matching the correct body text on line 189-192 and extracted [P1] Eq.(20)).


## docs/05_paper_deep_dives/paper_001_general_theory_phase_noise.md  (2 findings)

1. [MEDIUM|correctness] In the Eq.(22)-(24) Step-by-step note (line ~237), the algebra relating c0/c1 to c0/Γrms is inverted. The text reads: "$c_0/c_1\approx c_0/\Gamma_{rms}\cdot\sqrt2$ 的近似來自「對稱波形以 $c_1$ 為主，$\Gamma_{rms}^2\approx c_1^2/2$」". But the stated assumption Γrms² ≈ c1²/2 gives c1 = Γrms·√2, hence c0/c1 = (c0/Γrms)/√2, i.e. the √2 should DIVIDE, not multiply. As written it is off by a factor of 2. (The main Eq.(24) form c0²/(2Γrms²) ≈ (c0/c1)² and the worked number using c0/Γrms=0.1 → 5 kHz are both correct; only this explanatory aside is wrong.)
   → FIX: Change "$c_0/c_1\approx c_0/\Gamma_{rms}\cdot\sqrt2$" to "$c_0/c_1\approx \dfrac{c_0/\Gamma_{rms}}{\sqrt2}$" (equivalently c1 = √2·Γrms when Γrms²≈c1²/2), so the aside is consistent with the equality c0²/(2Γrms²)=(c0/c1)².

2. [LOW|citation] Key figures table (line ~250) lists "Fig. 4 | 182", but correction-spec C10 mandates Fig.4 → p.181 and I confirmed against general.pdf that the Fig.4 caption "(a) Impulse injected at the peak, (b) impulse injected at the zero crossing, and (c) ... in state-space" is on journal p.181 (PDF page index 2); p.182 only contains a body-text reference to Fig.4. The C10 fix (p.182→p.181) did not land on this page's figure table.
   → FIX: In the Key figures table row, change the Fig. 4 page cell from 182 to 181, per _CORRECTION_SPEC.md C10 (verified: Fig.4 caption is on journal p.181).


## docs/05_paper_deep_dives/paper_003_injection_locking_part1.md  (2 findings)

1. [MEDIUM|citation] The classic Adler equation is attributed to the wrong paper section. Lines 63 and 94 cite it as '[P3] Sec. II'. Verified in the PDF: Adler's equation is Eq.(15) inside Section III 'SURVEY OF EXISTING MODELS' (journal p.2111). Section II is 'PRELIMINARIES' (pp.2109-2110), which does NOT contain Adler's equation. The page number p.2111 and equation number Eq.(15) are correct; only the section number is wrong. This is conspicuous because the page repeatedly advertises that its core citations are 'verbatim-verified against the original PDF'.
   → FIX: Change line 63 '**Original formula**（[P3] Sec. II, 約 p.2111，對照原文 Eq.(15)）' and line 94 '經典 Adler 為標準結果（[P3] Sec. II, p.2111 回顧 Adler [20]）' to 'Sec. III' (Adler's Eq.(15) lives in Section III 'SURVEY OF EXISTING MODELS', p.2111; the PDF header on p.2111 reads 'III. SURVEY OF EXISTING MODELS').

2. [MEDIUM|citation] In the 'Key figures' table the page number for Fig. 6 is wrong. Lines 176-178 list 'Fig. 6 | 2114 | block diagram：注入電流的諧波被 ISF 的諧波濾波...'. Verified in the PDF: Fig. 6 ('Decomposing the ISF into its spectral components, emphasizing how the injection waveform is filtered in the formation of the lock characteristic') is on journal p.2113, not p.2114. (Fig. 7, the time-domain lock characteristic, is correctly on p.2114.) The content description for Fig. 6 is correct; only its page number is wrong.
   → FIX: In the table row for Fig. 6, change the 頁 column from '2114' to '2113'. Fig. 6's caption sits on p.2113 alongside Fig. 5 and the Eq.(30)-(32) text; Fig. 7 begins p.2114.


## docs/05_paper_deep_dives/paper_004_injection_locking_part2.md  (1 findings)

1. [MEDIUM|citation] The displayed 'augmented pulling / amplitude-corrected Adler' equation (lines 105-110) is cited as '[P4] Eq.(21)，正弦形式 Eq.(22), p.2126', and the verification note (line 114) repeats '[P4] Eq.(21)，正弦形式 Eq.(22), p.2126…逐字確認'. But the equation actually shown — dθ/dt = (ω0−ωinj) − [½(Iinj/qmax)sinθ] / [1 + ½(Iinj·τ0/qmax)cosθ], with a MINUS sign, sinθ numerator, and the τ0 factor — is verbatim [P4] Eq.(27) on p.2128 (the ideal-LC specialization obtained after substituting the quadrature angles ∠90°/∠0 from Eq.(26)). The PDF's Eq.(21)/(22) on p.2126 are the GENERAL forms with a PLUS sign and cos(θ+∠Γ̃₁)/cos(θ+∠Λ₁) phase-offset terms — not the equation printed here. So the page shows Eq.(27) p.2128 but labels it Eq.(21)/(22) p.2126, and claims that label was 'verified verbatim'. The round-1 spec (C3) only fixed the general Eq.(18)-(22) numbers and never touched Eq.(27), so this mislabel is not a covered item.
   → FIX: Relabel the displayed equation to its true source: '[P4] Eq.(27), p.2128（ideal-LC 特例，由 Eq.(26) 的 ∠90°/∠0 代入 Eq.(22) 得到）'. PDF Eq.(27) reads exactly 'dθ/dt = ω0 − ωinj − [½ (Iinj/qmax,0) sinθ] / [1 + ½ τ0 (Iinj/qmax,0) cosθ]'. The general Eq.(22) p.2126 is instead 'dθ/dt = ω0 − ωinj + [½ Iinj|Γ̃₁| cos(θ+∠Γ̃₁)] / [1 + ½ Iinj|Λ₁| cos(θ+∠Λ₁)]' (note PLUS sign, cos with phase offset). Update the line-114 verification note so the 'p.2126 …逐字確認' wording matches whichever equation is actually shown (it currently asserts verbatim verification of Eq.(21)/(22) p.2126 for an equation that is Eq.(27) p.2128).


## docs/06_design_insights/symmetry.md  (2 findings)

1. [MEDIUM|regression] Line 151 (knobs table, first row) contains a corrupted word: "differential（差動）топology". The token "топology" mixes Cyrillic homoglyphs with Latin: т (U+0442), о (U+043E), п (U+043F) followed by Latin "ology". It renders almost identical to "topology" but the first three glyphs are wrong code points — a text-corruption defect (likely introduced by a round-1 edit) that a content/number lens does not catch. It will also break any in-page text search for "topology".
   → FIX: Replace the Cyrillic-contaminated "топology" with ASCII "topology": the cell should read "differential（差動）topology". Verify with grep -P "[\x{0400}-\x{04FF}]" that no other Cyrillic remains on the page (this is currently the only hit).

2. [LOW|pedagogy] Example 2 (line 202) pins the −20 dB comparison at "固定 offset（例如 $\Delta f=10$ kHz、仍在 1/f³ 區）". That parenthetical is only true for the ORIGINAL asymmetric waveform (corner 320 kHz, so 10 kHz < corner). For the IMPROVED waveform the corner drops to 3.2 kHz (computed two lines above), so Δf=10 kHz is now ABOVE the new corner and lies in the 1/f² region, not the 1/f³ region. The −20 dB result itself is correct (it compares the 1/f³ asymptote heights, which scale as c₀²), but the "仍在 1/f³ 區" qualifier can mislead a careful student into thinking 10 kHz stays in the 1/f³ band for both waveforms.
   → FIX: Reframe the offset as the extrapolated 1/f³ skirt level rather than asserting 10 kHz stays in-band for both. E.g. change "在固定 offset（例如 $\Delta f=10$ kHz、仍在 1/f³ 區）上 close-in phase noise 降了多少" to something like "比較兩條 1/f³ 漸近線（外推 skirt）的高度差" or note explicitly that after improvement the 3.2 kHz corner means 10 kHz now sits in the 1/f² region, so the −20 dB refers to the extrapolated 1/f³ asymptote.


## docs/06_design_insights/tank_swing.md  (1 findings)

1. [MEDIUM|correctness] The dimension-check block at lines 69–73 (the '整個括號要無因次給 dBc/Hz' check) contains a mathematically wrong, self-contradictory final equality. The chain correctly reduces to (C²s⁻²/Hz)/(C²s⁻²) = 1/Hz, but the page then writes `=\dfrac{1}{[\text{Hz}]}\cdot[\text{Hz}]^{-1}`, which equals Hz⁻² — an extra spurious 1/Hz factor that contradicts the very conclusion (per-Hz) the line is trying to reach. The trailing prose `……（i_n²/Δf 本身已是 per-Hz）最終得 per-Hz` then hand-waves back to the correct answer, but the written math is wrong and reads as garbled. This is jarring on a page that markets its rigorous dimension checks, and the cleaner duplicate check at lines 193–195 gets the same reduction right (it stops cleanly at 1/[Hz]).
   → FIX: Fix the last equality so it terminates at 1/[Hz] rather than (1/Hz)·(Hz)⁻¹. Replace `=\dfrac{[\text{C}^2\text{s}^{-2}/\text{Hz}]}{[\text{C}^2\text{s}^{-2}]}=\dfrac{1}{[\text{Hz}]}\cdot[\text{Hz}]^{-1}` … with `=\dfrac{[\text{C}^2\text{s}^{-2}/\text{Hz}]}{[\text{C}^2\text{s}^{-2}]}=\dfrac{1}{[\text{Hz}]}`, then note the ratio P_new/P_old itself is dimensionless so 10log₁₀ gives dB; the per-Hz of i_n²/Δf is what makes the absolute L per-Hz (dBc/Hz). Mirror the wording already used correctly at lines 193–195 and drop the garbled `……` tail.


## docs/06_design_insights/pll_noise_budget.md  (1 findings)

1. [MEDIUM|correctness] Step 3 crossover paragraph states the transfer-function values at f=f_n incorrectly. The text reads: "$\zeta=0.707$ 附近 $\lvert H_{lp}\rvert^2$、$\lvert H_{hp}\rvert^2$ 在 $f_n$ 處各約 $1$（兩者在此皆 $\sim$ 0 dB），總和會有個輕微的鼓包". Evaluating the page's own formulas at f=f_n (zeta=0.707) gives |H_lp|^2 = 1.50 (+1.76 dB) and |H_hp|^2 = 0.50 (-3.0 dB), NOT "各約 1 / 兩者在此皆 ~0 dB". The point where |H_lp|^2 = |H_hp|^2 (both ~0.85, -0.7 dB) is at f/f_n ~ 1.55, not at f_n; and |H_lp|^2 peaks at ~1.62 (+2.1 dB) near f/f_n~0.79. The qualitative peaking conclusion is fine (the sum at f_n is exactly 2.0 = +3 dB, which is the bump), but the stated individual magnitudes are wrong and could mislead a student into thinking both transfers equal 1 at f_n.
   → FIX: Replace "在 $f_n$ 處各約 $1$（兩者在此皆 $\sim$ 0 dB）" with the correct values, e.g. "在 $f_n$ 處 $\lvert H_{lp}\rvert^2\approx1.5$（+1.76 dB）、$\lvert H_{hp}\rvert^2\approx0.5$（−3 dB），兩者之和 $\approx2$（+3 dB）——這就是 $\zeta=0.707$ 下的輕微鼓包；兩條曲線真正相等（各 $\approx0.85$，−0.7 dB）發生在 $f\approx1.55\,f_n$". This keeps the existing 'slight peaking' conclusion while fixing the numbers.


## docs/06_design_insights/real_oscillator_topologies.md  (3 findings)

1. [MEDIUM|regression] Line 121 contains a broken/placeholder math expression that renders as nonsense: `這支 ISF 的 $\Gamma_{rms}=\sqrt{\tfrac12(c_0/ \text{...})}$`. The `(c_0/ \text{...})` is a literal unfinished placeholder with `...` inside a square root — it is not a valid formula for Γrms and gives the reader no usable expression. This looks like an editing artifact that was never completed.
   → FIX: Replace with the correct Parseval-based rms for the stated tail ISF $\Gamma_{tail}=\tfrac{c_0}{2}+c_{1,res}\cos\theta+c_2\cos2\theta$, namely $\Gamma_{rms}=\sqrt{(c_0/2)^2+\tfrac12(c_{1,res}^2+c_2^2)}$. Plugging $c_0=0.30,\,c_{1,res}=0.10,\,c_2=0.55$ gives $\sqrt{0.0225+0.5(0.01+0.3025)}=\sqrt{0.179}\approx0.42$, not 0.45.

2. [MEDIUM|correctness] The tail-ISF $\Gamma_{rms}\approx0.45$ used on lines 121, 141, 149 and in worked-example 1 (lines 159, 187) is internally inconsistent. From the page's own stated coefficients (c0=0.30, c1,res=0.10, c2=0.55) the rms is $\sqrt{(c_0/2)^2+\tfrac12(c_{1,res}^2+c_2^2)}=0.423$. The page even says '$\Gamma_{rms}\approx0.45$（見圖標）', but lab_21_topology_isf.py computes the figure label via `gamma_rms(theta, gamma_tail)`, which for those exact coefficients returns 0.42 (verified). So the cited figure shows ~0.42, not 0.45. This 0.45 then propagates into the headline result: with the consistent 0.423 the 1/f³ corner is $\omega_{1/f}\cdot c_0^2/(2\Gamma_{rms}^2)=2\pi\times10^6\times0.30^2/(2\times0.423^2)\Rightarrow\Delta f\approx251$ kHz, not the stated 222 kHz — a ~13% error on the worked example's flagship number.
   → FIX: Set $\Gamma_{rms}=0.42$ consistently on lines 121, 141, 149 and in Example 1 (lines 159, 187), and recompute: ratio $c_0^2/(2\Gamma_{rms}^2)=0.09/(2\times0.1779)=0.253$, corner $\Delta f_{1/f^3}=2\pi\times10^6\times0.253/(2\pi)\approx253$ kHz; update the '$\approx222$ kHz' in the worked example, the python comment, the summary table (line 424) and the 重點回顧 (line 453). Alternatively, if 0.45 is intended, change the coefficients so the rms actually equals 0.45 and regenerate the figure — but the figure currently shows ~0.42, so aligning to 0.42 is simpler.

3. [LOW|correctness] Example 3 (ring stage) states on line 368-369 '對 $N=5$ 的三角 toy，數值上 $\Gamma_{rms}\approx0.30$（toy 量級；精確值見 `gamma_rms(theta, gamma_triangular(theta, 5))`）' and then computes L with 0.30 (lines 374, 398). But that exact function call returns 0.258 (verified), not 0.30. The text calls 0.30 the '精確值' (precise value) from that call, which is misleading; with 0.258 the result is −137.7/−130.7 dBc/Hz vs the stated −136.4/−129.4 (a ~1.3 dB shift). The whole example is hedged as '量級' (order of magnitude), which softens this, but the '精確值見…' phrasing directly contradicts the value used.
   → FIX: Either change 0.30 to the actual computed 0.26 and recompute L1≈−137.7, L5≈−130.7 dBc/Hz (and the python `# ~0.26` comment), or drop the word '精確值' and keep 0.30 explicitly as a rounded order-of-magnitude figure so it no longer claims to equal the function call's output.


## docs/06_design_insights/measurement_and_spurs.md  (2 findings)

1. [MEDIUM|correctness] The spectrum-analyzer direct-method formula (line 43) SUBTRACTS the 2.5 dB log-detector correction: $\mathcal{L}(\Delta f)=10\log_{10}(P_{SSB}/P_{carrier})-10\log_{10}(\text{RBW}/1\,\text{Hz})\ -\ 2.5\,\text{dB}$. The well-established SA noise-measurement convention is the opposite sign: a log amplifier with video/sample averaging UNDER-displays Gaussian noise by ~2.5 dB (the classic 2.51 dB Rayleigh-log skew), so the correction to recover true noise power is to ADD ~2.5 dB (Keysight/Agilent AN-1303 spectrum-analysis fundamentals). Line 48's prose 'log 檢波器平均約 −2.5 dB' correctly describes the under-read, but folding it into the final formula with a minus sign double-counts the direction and yields an $\mathcal{L}$ that is ~5 dB too optimistic relative to the standard correction. The page hedges with 'toy/illustrative 的近似', which softens but does not remove the inverted-sign correctness issue in a graduate teaching formula.
   → FIX: Flip the sign to add the correction (or net it correctly): e.g. '$\mathcal{L}(\Delta f)=10\log_{10}(P_{SSB}/P_{carrier})-10\log_{10}(\text{ENBW}/1\,\text{Hz})\ +\ 2.5\,\text{dB}$', and reword line 48 to 'log 檢波器對高斯雜訊低估約 2.5 dB，需「加回」2.5 dB；再用 ENBW 取代 RBW 做頻寬歸一'. Keep the toy/illustrative caveat but make the sign match the conventional noise-marker correction.

2. [MEDIUM|pedagogy] Delay-line discriminator derivation has a step that jumps. Line 92 defines the transfer function as $|H(\Delta f)|^2=(2\pi\tau_d)^2\,\operatorname{sinc}^2(\Delta f\,\tau_d)$ and the surrounding prose (line 89) explicitly says it maps frequency noise $S_{\Delta f}$ to the output. But line 98 then inverts to phase noise as $S_\phi(\Delta f)=S_v/[K_\phi^2\,(2\pi\,\Delta f\,\tau_d)^2\,\operatorname{sinc}^2(\Delta f\,\tau_d)]$, where an extra factor of $\Delta f^2$ has appeared inside the $(2\pi\,\Delta f\,\tau_d)^2$ term with no explanation. The missing bridge is the frequency-to-phase relation $S_{\Delta f}(\Delta f)=\Delta f^2\,S_\phi(\Delta f)$ (a frequency discriminator senses frequency fluctuation, which is the derivative of phase). On a page whose stated purpose is step-by-step teaching, two consecutive equations differing by an unexplained $\Delta f^2$ will confuse a careful reader.
   → FIX: Insert one line between the two equations stating the frequency↔phase bridge, e.g. '鑑頻器感測的是頻率起伏 $\delta f$，而頻率是相位的微分，故 $S_{\delta f}(\Delta f)=\Delta f^2\,S_\phi(\Delta f)$；把它代回 $|H|^2$ 即得分母多出的 $\Delta f^2$（故 $(2\pi\tau_d)^2\to(2\pi\,\Delta f\,\tau_d)^2$）。' Then line 98 follows without the unexplained jump.


## docs/99_appendix/ltv_htm.md  (2 findings)

1. [MEDIUM|correctness] Step 5 (line 310) states the equality chain $\sum_k|\tilde c_k|^2=\tfrac12\sum_n c_n^2=\Gamma_{rms}^2$, but the MIDDLE term is wrong and breaks the chain. Using $\tilde c_0=c_0/2$ and $\tilde c_{\pm k}=\tfrac12 c_k e^{\pm j\theta_k}$: $\sum_k|\tilde c_k|^2 = (c_0/2)^2 + 2\sum_{k\ge1}(c_k/2)^2 = c_0^2/4 + \tfrac12\sum_{k\ge1}c_k^2$, which equals $\Gamma_{rms}^2$ (left = right, both correct). But $\tfrac12\sum_n c_n^2 = c_0^2/2 + \tfrac12\sum_{k\ge1}c_k^2$ OVER-counts the DC term by $c_0^2/4$, so it is NOT equal to either. Numerically verified with c0=0.4,c1=1,c2=0.35,c3=0.18: $\sum_k|\tilde c_k|^2=\Gamma_{rms}^2=0.61745$ but $\tfrac12\sum_n c_n^2=0.65745$. This is exactly the DC-double-count subtlety that round-1 C15 fixed in lab_05 (DC enters Parseval as $(c_0/2)^2$, giving the canonical $(c_0/2)^2+\sum_{n\ge1}c_n^2=2\Gamma_{rms}^2$); this page still carries the uncorrected $\tfrac12\sum_n c_n^2$ bridge. (The figure caption at line 188 is correct because it omits the middle term and writes only $\sum_k|\tilde c_k|^2=\Gamma_{rms}^2$.)
   → FIX: Drop the erroneous middle term. Replace '$\sum_k|\tilde c_k|^2=\tfrac12\sum_n c_n^2=\Gamma_{rms}^2$（差個 normalization）' with the DC-correct form, e.g. '$\sum_k|\tilde c_k|^2=(c_0/2)^2+\tfrac12\sum_{n\ge1}c_n^2=\Gamma_{rms}^2$（DC 項以 $(c_0/2)^2$ 計入，與 lab_05 Parseval 修正一致；對應 [P1] Eq.(20) 的 $(c_0/2)^2+\sum_{n\ge1}c_n^2=2\Gamma_{rms}^2$）'. Do NOT write $\tfrac12\sum_n c_n^2$, which double-counts DC.

2. [MEDIUM|correctness] Step 5 (line 290-294) has an index/sign slip that contradicts the page's own conventions. The prose says noise '落在 $f=-kf_0+\Delta f$ ... 被 $H_{-k}$ 搬到 baseband 的 $\Delta f$', and the equation uses the factor $\tilde c_{-k}$. But by the HTM relation established in Step 3 (line 145, $y=\sum_m H_m(f)\,e^{j2\pi(f+mf_0)t}$), an input at $f=-kf_0+\Delta f$ lands at baseband $\Delta f$ only via $m=+k$ (since $(-kf_0+\Delta f)+kf_0=\Delta f$), i.e. the folding gain is $H_{+k}=\tilde c_k$, NOT $H_{-k}=\tilde c_{-k}$. Folding by $H_{-k}$ would send it to $-2kf_0+\Delta f$. It also contradicts the $\mathbf g_\phi$ vector immediately below (line 306-309), where 'component $k$ = noise band at distance $kf_0$ → gain $\tilde c_k$'. The final magnitude is unaffected because $|\tilde c_{-k}|=|\tilde c_k|=c_k/2$, which is why the boxed $\frac{|c_k|}{2q_{max}}$ result and the Eq.(16/17) reconciliation are still correct — hence medium, not high. But a student tracing the indices hits a genuine contradiction.
   → FIX: Make the subscript consistent with the page's own HTM convention and the $\mathbf g_\phi$ vector. In line 290 change '被 $H_{-k}$ 搬到' to '被 $H_{k}$ 搬到', and in the boxed/inline gain at line 294 change the under-braced factor $\tilde c_{-k}$ to $\tilde c_{k}$ (so it reads $\frac{1}{q_{max}}\cdot\underbrace{\tilde c_{k}}_{\text{ISF 搬移}}\cdot\underbrace{\frac{1}{j2\pi\Delta f}}_{\text{積分器}}$). Optionally add one clause: 'noise 為實訊號，$\pm$ band 對稱，$|\tilde c_{k}|=|\tilde c_{-k}|$，故取模後與符號無關'.


## docs/99_appendix/derivation_leeson.md  (2 findings)

1. [MEDIUM|correctness] Step 1, line 52 ("那個 2 哪來") gives a self-contradictory physical origin for the leading 2 in L_floor = 2FkT/P_s. The sentence first invokes AM/PM equipartition — "雜訊同時擾動振幅與相位，等分到相位的是一半的雜訊功率密度" — which is a factor of 1/2 (phase keeps HALF the power), then claims that this same mechanism, "因為單邊帶（SSB）記帳", is what yields the factor of 2 in 2FkT/P_s. A ÷2 (equipartition) and a ×2 (SSB convention) are conflated as "同一類 factor-of-2", so a careful reader following the argument would derive L_floor = (1/2)FkT/P_s, the opposite sign of exponent. The widely-cited Leeson floor is actually L_floor = FkT/P_s (the AM/PM split and the SSB ×2 cancel); the explicit 2 in the bracketed Leeson form is a pure bookkeeping convention, not the equipartition factor. The cross-reference to [P1] Eq.(21)'s 4Δω² vs time-domain 2Δω² is a DIFFERENT factor-of-2 (Parseval/SSB packaging in the ISF derivation) and is not the same object as the AM/PM equipartition, so equating them adds to the confusion.
   → FIX: Separate the two mechanisms instead of fusing them. Suggested replacement for the bullet: "那個 2 屬於 Leeson 模型的記帳慣例（並非唯一寫法，依文獻而異）。物理上有兩步、方向相反：(1) AM/PM 等分——熱雜訊同時擾動振幅與相位，相位只分到一半功率（×½）；(2) 單邊帶（SSB）記帳把雙邊功率折算成單邊（×2）。兩者相抵後常見的地板寫法是 FkT/P_s；本式取 2FkT/P_s 是把 SSB 慣例顯式留在前置常數的版本。這與 [P1] Eq.(21) 的 4Δω² vs 時域 2Δω² 屬於同一類『慣例性 factor-of-2』，但不是同一個因子。" The key correction is to NOT claim the equipartition ÷2 produces the leading ×2.

2. [LOW|pedagogy] Step 2 (lines 57-65) presents |H(Δω)|² ∝ (ω0/2QΔω)² as a tank transfer function and separately gives the correct physical reason (phase has no restoring force → integration → 1/Δω²), but never reconciles the two: a passive RLC tank's voltage transfer rolls off as 1/Δω (i.e. |H|²∝1/Δω²) only because of its bandpass slope, which by itself does NOT explain why an AUTONOMOUS oscillator's phase noise is 1/Δω². The page correctly attributes the real cause to the marginal phase direction (λ1=0) but leaves the impression that the tank-filter picture and the phase-integration picture are the same derivation. This is the well-known weakness of Leeson's heuristic that ISF later repaired.
   → FIX: Add one clause noting the tension, e.g. after line 64: "嚴格說，被動 tank 的帶通斜率本身給的是 amplitude 抑制；自治振盪器相位之所以是 1/Δω²，根因是相位中性方向（λ1=0）被閉迴路積分——Leeson 用 tank Q 把這件事『包』起來，但沒有真正導出它，這正是 [P1] 後來補上的嚴格之處。" This sharpens why Leeson is semi-empirical and previews the ISF advance.


## docs/03_isf_core_theory/exercises.md  (2 findings)

1. [MEDIUM|pedagogy] Exercise 6's dimension check (line 292) is garbled and does not cleanly justify the claimed result. It writes: "$\dfrac{[\text{A}]\cdot(\text{無因次})}{[\text{C}]\cdot[\text{rad/s}]}=\dfrac{\text{A}}{\text{A·s}}\cdot\dfrac{\text{s}}{\text{rad}}=\text{rad}^{-1}\cdot\text{rad}$... 化簡 $\phi$ 無因次（rad）✓". The intermediate "$\text{rad}^{-1}\cdot\text{rad}$" has an unexplained extra factor of rad (the s/rad term already gives rad^-1), the trailing "..." hides the actual cancellation, and the conclusion "無因次（rad）" conflates dimensionless with rad. Worked out properly: A/(C·(rad/s)) = A/(A·s·(1/s)) = dimensionless (treating rad as dimensionless), so phi is dimensionless. The shown arithmetic does not transparently land there. This violates the page's own stated standard (逐步代入...怎麼檢查 dimension) and would confuse a careful student.
   → FIX: Rewrite line 292 cleanly, e.g.: phi_n = I0·c_n/(2 q_max)·sin(...)/Δω has units [A]·1 / ([C]·[rad/s]); with C=A·s and rad dimensionless, = A / (A·s · 1/s) = dimensionless ✓ (phase in rad). Drop the stray '·rad' and the trailing '...'.

2. [LOW|pedagogy] Exercise 5 instructs the student to use the (c0/c1)^2 approximation of the 1/f^3 corner ([P1] Eq.(24)) with the asymmetric inputs c0=0.2, c1=1.0, yielding 40 kHz. But the page's own header (line 22) and the standard exact form is Δω_{1/f^3}=ω_{1/f}·c0^2/(2 Γrms^2). With these inputs and a DC+fundamental ISF, Γrms^2=(c0^2+c1^2)/2=0.52 (not c1^2/2=0.5), so the exact corner is 38.5 kHz vs the approximation's 40 kHz — a ~4% gap precisely because c0=0.2 is not negligible. The page never states the applicability condition that (c0/c1)^2 holds only when the ISF is fundamental-dominated (Γrms^2≈c1^2/2), which is exactly the regime the asymmetric case strains. (C11 sanctions the 40 kHz number, so this is not a regression — only a missing failure-condition note.)
   → FIX: Add a one-line caveat in Ex5 solution, e.g.: 近似式 (c0/c1)^2 假設 ISF 由基波主導（Γrms²≈c1²/2）；此處 c0=0.2 不可忽略，精確式 c0²/(2Γrms²)（Γrms²=0.52）給 ≈38.5 kHz，與近似 40 kHz 差約 4%。對稱化後（c0=0.02）兩者幾乎一致。


## docs/06_design_insights/exercises.md  (2 findings)

1. [MEDIUM|citation] Line 20 cites the ring frequency relation f_0 = 1/(2N\tau_D) as "[P2] Eq.(14), p.794". This is a leftover of the round-1 C9 fix that did not propagate to this page. Verified against the PDF (jitter_ring.pdf, journal p.794, PDF page index 4): Eq.(14) is the stage-delay-vs-rise-time relation, and Eq.(15) is the period relation 'The period is 2N times longer than a single stage delay' from which f_0 = 1/(2N\tau_D) follows. Correction-spec C9 and _AUTHORING_SPEC item 21 both mandate Eq.(15). The other occurrence on line 183 carries no equation number, so only line 20 is wrong.
   → FIX: On line 20 change 'ring 頻率 $f_0=\dfrac{1}{2N\tau_D}$（[P2] Eq.(14), p.794）' to cite '[P2] Eq.(15), p.794' (Eq.(14) is the stage-delay definition; Eq.(15) is the period relation 2π=2N·t̂_D ⇒ f_0=1/(2N τ_D)), matching C9 / authoring-spec item 21.

2. [MEDIUM|correctness] Exercise 1's problem statement (lines 31-32) is internally inconsistent. It states the oscillator currently has L(1 MHz) = -140 dBc/Hz and explicitly says to plug into Eq.(21) with Γ_rms=0.7, q_max=1 pC, S_i=1e-23 A^2/Hz. But Eq.(21), L=10log10[(Γ_rms^2/q_max^2)(S_i/(4Δω^2))] with Δω=2π·1e6, yields -135.1 dBc/Hz, not -140 (the /2 time-domain convention gives -132.1; no standard convention gives -140). To actually obtain -140 you would need S_i≈3.2e-24 A^2/Hz or Γ_rms≈0.40. The exercise's answer is unaffected (it is the pure -9 dB ratio, independent of the absolute level), but the stated starting number contradicts its own inputs and a careful student verifying via Eq.(21) — exactly the dimension-check discipline the page preaches — will get -135, not -140.
   → FIX: Make the starting point self-consistent: either change S_i to ≈3.2e-23... (correction: ≈3.2e-24 A^2/Hz) so Eq.(21) gives -140 dBc/Hz with Γ_rms=0.7/q_max=1 pC, or change the stated level to -135 dBc/Hz, or drop the explicit '套 Eq.(21)' and present -140 as a given spec not derived from the listed parameters. Verified: with current inputs Eq.(21) → 10*log10((0.7**2/1e-24)*(1e-23/(4*(2*np.pi*1e6)**2))) = -135.08 dBc/Hz.


## docs/04_simulation_labs/worked_examples.md  (2 findings)

1. [MEDIUM|citation] Example D3 step (a) cites the ring-frequency relation with the WRONG equation number: line 630 reads "第 1 步（a），ring 頻率（[P2] Eq.(14)）：$f_0=\dfrac1{2N\tau_D}\Rightarrow N=\dfrac1{2f_0\tau_D}$。". Verified in the PDF (jitter_ring p.794): "The period is 2N times longer than a single stage delay (15)", i.e. $f_0=1/(2N\tau_D)$ is [P2] Eq.(15); Eq.(14) is the separate stage-delay/rise-time relation $\hat t_D=\eta/f'_{max}$. This is exactly correction C9/C18. Round-1 already fixed this elsewhere (paper_summary_table.md, lc_vs_ring.md, references.md all now say Eq.(15), and build_report.md logs "$f_0=1/(2N\tau_D)$ 改引 Eq.(15)"), so this page is a leftover regression that now disagrees with the fixed pages.
   → FIX: On line 630 change "（[P2] Eq.(14)）" to "（[P2] Eq.(15), p.794）" so it matches the verified PDF and the round-1-fixed sibling pages (paper_summary_table.md line 25, lc_vs_ring.md line 233, references.md line 49).

2. [MEDIUM|regression] Stale "⚠️ 常數待查" markers on the ring $\Gamma_{rms}\propto N^{-3/4}$ result survive on this page (line 636 "（[P2] Eq.(16)，⚠️ 常數待查；scaling 為主）", line 654 "見 [P2] p.795 的 FOM，⚠️ 常數待查", and the honesty note line 785 "ring $\Gamma_{rms}\propto N^{-3/4}$ 常數待查（⚠️）"). Per correction C8/C16 the Eq.(16) constant is verified ($\Gamma_{rms}=\sqrt{(2\pi^2/3\eta^3)(1/N^{1.5})}$, η not γ, [P2] p.794, 已核實) and these warnings should be closed. Round-1 closed them on the sibling pages (e.g. lab_09_design_tradeoffs.md and exercises.md now read "[P2] Eq.(16), p.794，已核實"), so this page now contradicts the rest of the site's verification status. (Note line 654 also cites "p.795 的 FOM" while the FOM Eq.(23) and the N-independence conclusion are on p.796 — p.795 is only the Section-V start.)
   → FIX: Replace the three "⚠️ 常數待查" occurrences with the verified status: line 636 -> "（[P2] Eq.(16), p.794，已核實；scaling 為主）"; line 654 -> scope the residual caveat to the prefactor only and drop "⚠️ 常數待查" from the N-independence conclusion (which C16 calls solid: [P2] Sec.V, Eq.(23)/(25), p.796), e.g. "見 [P2] Sec.V FOM (Eq.23/25, p.796)"; line 785 -> remove "常數待查（⚠️）" and keep only the toy-scaling/14.3-dB-is-illustrative caveat.


## docs/03_isf_core_theory/rms_isf.md  (1 findings)

1. [LOW|pedagogy] Step 5 (lines 148-153) gives the factor-of-2 SSB note as S_phi(f)=Gamma_rms^2 S_i/(q_max^2 (2*pi*f)^2) and L = Gamma_rms^2 S_i/(2 q_max^2 Delta_omega^2), then says [P1] Eq.(21) uses /(4 Delta_omega^2). The numerical worked example (Example 2, lines 181-190) uses the Eq.(21) /(4 Delta_omega^2) convention to get -148 dBc/Hz, which is correct and recomputes exactly. But a careful student could read Step 5 as implying the 'clean time-domain' answer (with /2) is the 'right' one and then be unsure which convention the -148 number uses. The page never states explicitly that the worked number deliberately follows the Eq.(21) /4 convention.
   → FIX: Add one half-sentence to Example 2 tying it to Step 5, e.g. after line 179 '用 Eq.(21)（即 SSB /(4Delta_omega^2) 慣例，見第 5 步的 factor-of-2 註記）'. This removes any ambiguity about which of the two conventions the -148 dBc/Hz uses.


## docs/06_design_insights/waveform_slope.md  (1 findings)

1. [LOW|citation] Line 104 attributes the claim 'ring 的 ISF 集中在 transition 附近' to '[P2] Fig. 5'. In [P2] (jitter_ring.pdf, p.793) Fig. 5 is captioned 'ISF for ring oscillators of the same frequency with different number of stages' — it plots ISF curves for varying N (showing the peak/lobe narrowing with N). The figure that actually pairs the near-square ring waveform with its transition-concentrated ISF is Fig. 6, p.793, captioned 'Approximate waveform and ISF for ring oscillator.' So the single cleanest support for 'concentrated near transition' is Fig. 6, while Fig. 5 best supports the N-dependence. NOTE: this is NOT a page-local slip — the identical '[P2] Fig. 5' citation for this same claim appears site-wide (paper_002 line 50, lc_vs_ring line 28, python_environment line 129), so it is a cross-cutting convention that a cross-cutting lens owns, not a regression introduced here. Flagging only at LOW for completeness; Fig. 5 is defensible since its narrowing lobes do visually show transition-concentration.
   → FIX: If the site standardizes this, change '集中在 transition 附近（[P2] Fig. 5）' to cite Fig. 6 (waveform+ISF overlay) for the concentration claim, e.g. '集中在 transition 附近（[P2] Fig. 6, p.793；Fig. 5 顯示峰值/lobe 隨 N 變窄）', and apply the same edit to the other 3 pages so they stay consistent.


## docs/06_design_insights/varactor_tuning_supply_pushing.md  (1 findings)

1. [LOW|correctness] Front-matter description writes the headline equation as `S_φ=K_VCO²·S_v/Δω²`, which is the dimensionally-incorrect pairing: with Δω in the denominator the numerator must carry the (2π)² factor, i.e. `(2πK_VCO)²S_v/Δω²`. The body's boxed equation (line 71) gets this exactly right, presenting BOTH the `(2πK_VCO)²S_v/Δω²` form and the cancelled `K_VCO²S_v/Δf²` form. So the description mismatches the (correct) body by a factor of (2π)². It lives only in the SEO/listing metadata, not in the taught content, hence low severity.
   → FIX: In the front-matter description change `S_φ=K_VCO²·S_v/Δω²` to the cancelled Hz form `S_φ=K_VCO²·S_v/Δf²` (matching the right-hand side of the boxed Eq. at line 71), or to `(2πK_VCO)²·S_v/Δω²` if the rad/s form is preferred.


## Four new ISF teaching figures: static/figures/impulse_phase_decomposition.png (simulations/fig_impulse_decomp.py), static/figures/htm_band_folding.png (simulations/fig_htm_bandfold.py), static/figures/device_noise_isf_bands.png (simulations/fig_device_noise_bands.py), static/figures/lock_characteristic_omega.png (simulations/fig_lock_characteristic.py)  (1 findings)

1. [LOW|artifact] In static/figures/device_noise_isf_bands.png (bottom panel), the purple c2->carrier folding arrowhead terminates directly on top of the 'c1=1.00' text label, partially occluding the trailing '00' digits so the label reads as roughly 'c1=1.0' / 'c1=1.'. The value is still inferable (and the title plus code confirm c1=1.00), so this is a cosmetic overlap, not a wrong number. Verified by zooming into the carrier region of the rendered PNG.
   → FIX: In simulations/fig_device_noise_bands.py, separate the arrow terminus from the c1 label: e.g. nudge the c1 annotation text up/left (increase its +0.04 y-offset or set ha to right), or shorten/raise the purple FancyArrowPatch endpoint (currently both arrows terminate at y_arrow = max(c)*1.18, very close to the c1 marker at the carrier x). Re-run the script and confirm the '1.00' is fully visible.
